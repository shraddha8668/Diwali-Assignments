CREATE DATABASE IF NOT EXISTS login_db;

USE login_db;

CREATE TABLE Users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    login_id VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(50) NOT NULL
);

-- Sample user
INSERT INTO Users (login_id, password) VALUES ('admin', 'admin123');
